import { defineConfig } from "vite";

export default defineConfig({
  base: "/Experiment-1-loading-text-path-preloader/",
  build: {
    outDir: "dist",
    assetsDir: "assets",
  },
  publicDir: "public",
});
